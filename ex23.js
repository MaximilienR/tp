//mes variable 
var prompt = require ('prompt-sync')();
const table = prompt("veuillir votre nombre  ");

//mes boucles
for (let index = 0; index <11; index++) {
     console.log(table + "*"+index + "=" +index*table)
     
}